# Required (server-side)
YOUTUBE_API_KEY=YOUR_KEY_HERE
COOKIE_SECRET=change_me

# Optional local port for `vercel dev` (Vercel production ignores this)
# PORT=3000

# --- Caching options for serverless ---
# Middle column (YouTube/RSS mix) page-level cache:
YT_CACHE_TTL_MS=300000          # 5 minutes
YT_CACHE_EMPTY_TTL_MS=60000     # 1 minute for empty results
YT_CIRCUIT_TTL_MS=600000        # 10 minutes circuit breaker window

# Side panels (server responses)
TOP_TRENDS_TTL_MS=3600000       # 60 minutes (left column)
RIGHT_PANEL_TTL_MS=900000       # 15 minutes (right column)

# --- Optional persistent cache (Vercel KV / Upstash) ---
# Leave these blank and KV stays OFF.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional KV toggle (OFF by default). Enabling may incur costs (reads/writes/storage).
# Explicit KV toggle (OFF by default). Set to 1 AND provide KV_* to enable.
# ENABLE_KV= (empty) → OFF
# ENABLE_KV=0 → OFF
# ENABLE_KV=1 and valid KV_REST_API_URL + KV_REST_API_TOKEN → ON
# ENABLE_KV=1 but missing creds → OFF
ENABLE_KV=
#KV_NAMESPACE=

# Note: Do NOT prefix server env vars with VITE_. Vite only exposes VITE_* to the client bundle.
	